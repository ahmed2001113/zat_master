export const UserDatils =({user={}})=>{
    if(!user)console.log('no user exist ')
    else{
        return(
            <div>
                <h1>User Details</h1>
            </div>
        )
    }
    }